/**
 * setup_screen.h — "new game" screen: pick player count + starting life,
 * then start. Shown on boot and whenever the player wants to reset.
 */

#pragma once

void setup_screen_create();
void setup_screen_destroy();
